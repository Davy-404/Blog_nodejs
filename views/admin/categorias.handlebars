<h2>Lista de categorias:</h2>
<hr>
<a href="/admin/categorias/add"><button class="btn btn-success">Nova categoria</button>

{{#each categorias}}
    <div class="card mt-4">
        <div class="card-body">
            <h3>{{nome}}</h3>
            <small>Slug: {{slug}}</small> <br>
            <small>Data de criação: {{data}}</small> <br>
            <a href="/admin/categorias/edit/{{_id}}"><button class="btn btn-success mt-3">Editar categoria</button>
            <form action="categorias/deletar" method="POST">
                <input type="hidden" name="id" value="{{_id}}">
                <button type="submit" class="btn btn-danger mt-2">Excluir categoria</button>
            </form>
        </div>
    </div>
{{else}}
<h4 class="mt-2">Nenhuma categoria registrada!</h4>
{{/each}}